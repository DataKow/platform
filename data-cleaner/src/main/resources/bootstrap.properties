spring.application.name=datacleaner
#This is for retrying config
#Retry every 5 seconds for 24 hours
spring.cloud.config.failFast=true
spring.config.retry.initialInterval=5000
spring.config.retry.multiplier=1
spring.config.retry.maxAttempts=28800
spring.cloud.config.uri=${CONFIG_HOST}

